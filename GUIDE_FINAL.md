# Linear Code Classification - LP 기반 타이트한 상한 구현

## 🎯 핵심 결과

### ✅ 정확도: 100% 보장
- LP 상한은 유효한 해를 절대 제외하지 않음
- 모든 테스트에서 기본 방법과 동일한 격자점 개수 발견

### 📊 효율성: 32-66% 향상
| 파라미터 | 탐색 공간 감소 | 유효한 해 | 정확도 |
|---------|-------------|----------|--------|
| [7,3,3]_2 | 65.92% | 155 (동일) | 100% ✅ |
| [10,3,4]_2 | 32.18% | 1,239 (동일) | 100% ✅ |
| [12,3,5]_2 | 49.70% | 2,051 (동일) | 100% ✅ |

---

## 🚀 Google Colab 사용법

### 방법 1: 파일 업로드
```python
# 1. 파일 업로드
from google.colab import files
uploaded = files.upload()  # colab_complete.py 선택

# 2. 실행
%run colab_complete.py

# 또는
from colab_complete import compare_three_methods
compare_three_methods(n=10, k=3, q=2, d=4)
```

### 방법 2: 코드 직접 복사
1. `colab_complete.py` 파일 내용 전체를 복사
2. Colab 새 셀에 붙여넣기
3. 실행 (Shift+Enter)

---

## 📖 함수 사용법

### `compare_three_methods(n, k, q, d)`

**파라미터:**
- `n`: 부호 길이 (length)
- `k`: 차원 (dimension)
- `q`: 유한체 크기 (field size)
- `d`: 최소 거리 (minimum distance)

**예시:**
```python
# 작은 예시
compare_three_methods(n=7, k=3, q=2, d=3)

# 중간 예시
compare_three_methods(n=10, k=3, q=2, d=4)

# 큰 예시 (시간 오래 걸림)
compare_three_methods(n=15, k=3, q=2, d=5)
```

**출력:**
```
======================================================================
Linear Code Parameters: [n=10, k=3, d=4]_2
======================================================================

[방법 1: Phase 0 없이 (이론적)]
  - PG(2, 2)의 점 개수: 7
  - 이론적 전체 경우의 수: 8,008

[탐색 공간 분석]
  - 제약 없는 탐색 공간: 31,824 노드
  - LP 상한 적용 시: 21,584 노드
  - 탐색 공간 감소: 10,240 (32.18%)

[방법 2: 기본 Phase 0 (상한 없음)]
  - 유효한 격자점 개수: 1,239
  
[방법 3: LP Phase 0 (타이트한 상한)]
  - 상한 분포: [4]
  - 유효한 격자점 개수: 1,239

[검증 및 요약]
  ✅ 정확도: 100% (두 방법 모두 1,239개 발견)
  📊 효율성 비교:
     - 탐색 공간: 31,824 → 21,584 (32.18% 감소)
     - 유효한 해: 1,239 (동일)
     - 계산 효율: 32.2% 향상
======================================================================
```

---

## 🔬 알고리즘 설명

### LP 기반 상한 계산

각 x_P에 대해 다음 선형 프로그래밍 문제를 풀어서 상한 계산:

```
maximize: x_P

subject to:
    ∑x_Q = n                        (총 길이)
    ∑(Q∈H) x_Q ≤ n - d  for all H  (hyperplane 제약)
    x_Q ≥ 0              for all Q  (음이 아닌 정수)
```

**왜 정확도가 100%인가?**

LP 상한은 **필요조건**을 사용합니다:
- 만약 어떤 값이 LP 상한을 넘으면, **반드시** 어떤 hyperplane 제약을 위반
- 따라서 그런 값은 **절대로** 유효한 해가 될 수 없음
- LP 상한 이하의 값만 시도해도 **모든 유효한 해를 찾을 수 있음**

**예시:**
```
x_P1 + x_P2 ≤ 5
x_P2 + x_P3 ≤ 5  
x_P3 + x_P1 ≤ 5

LP로 계산하면:
- x_P1 ≤ 3 (단순히 5가 아니라 더 타이트!)
- x_P2 ≤ 3
- x_P3 ≤ 3

왜냐하면 세 제약을 모두 고려하면:
2(x_P1 + x_P2 + x_P3) ≤ 15
→ x_P1 + x_P2 + x_P3 ≤ 7.5
```

---

## 📈 성능 분석

### 탐색 공간 vs 유효한 해

| 지표 | 설명 | LP 효과 |
|-----|------|---------|
| **탐색 공간** | enumerate에서 방문하는 노드 수 | ✅ 32-66% 감소 |
| **유효한 해** | hyperplane 제약을 만족하는 격자점 | ✅ 동일 (100% 정확) |

**결론:**
- LP 상한으로 **더 빠르게** 같은 결과를 얻음
- 정확도 손실 **0%**

---

## ⚙️ 파라미터 권장 범위

### 실용적 범위 (1분 이내)
- q=2: k ≤ 4, n ≤ 20
- q=3: k ≤ 3, n ≤ 15
- q≥5: k ≤ 3, n ≤ 12

### 주의사항
- 점 개수 = (q^k - 1)/(q - 1)
- 점이 많을수록 계산 시간 급증
- LP 계산도 점 개수에 비례하여 증가

---

## 🔧 문제 해결

### scipy 설치 오류
```python
!pip install scipy
```

### 계산이 너무 느린 경우
1. 더 작은 파라미터로 테스트
2. k 값을 줄이기 (점 개수가 지수적으로 증가)
3. 진행 상황 표시 추가

### 메모리 부족
- k 값 감소
- 점 개수 확인: `ProjectiveSpace(k, q).num_points()`

---

## 📚 코드 구조

```
colab_complete.py
├── ProjectiveSpace 클래스
│   ├── generate_points()          # 사영공간의 점 생성
│   └── generate_hyperplanes()     # hyperplane 생성
│
├── count_without_phase0()         # Phase 0 없이 이론적 개수
├── compute_upper_bounds_lp()      # LP로 상한 계산
├── count_with_basic_phase0()      # 기본 Phase 0
├── count_with_lp_phase0()         # LP Phase 0
├── count_search_space()           # 탐색 공간 크기
└── compare_three_methods()        # 메인 비교 함수
```

---

## 🎓 이론적 배경

### 논문
Kurz, S. (2024). Computer classification of linear codes based on lattice point 
enumeration and integer linear programming. arXiv:2403.17509

### 핵심 개념
1. **사영공간**: 선형 부호를 PG(k-1, q)의 점들의 multiset으로 표현
2. **Hyperplane 제약**: 모든 hyperplane H에 대해 M(H) ≤ n - d
3. **LP 완화**: 정수 제약을 완화하여 타이트한 상한 계산

---

## 💡 추가 개선 아이디어

1. **동적 상한 업데이트**
   - 탐색 중에 상한을 동적으로 업데이트
   - Branch-and-bound 기법

2. **병렬화**
   - 여러 스레드로 탐색 가속
   - GPU 활용 가능성

3. **휴리스틱**
   - 유효한 해가 있을 가능성이 높은 영역부터 탐색
   - A* 알고리즘 응용

---

## 📞 문의 및 피드백

이 구현에 대한 질문이나 개선 아이디어가 있으시면 알려주세요!

---

## 📄 라이센스

MIT License - 자유롭게 사용, 수정, 배포 가능
